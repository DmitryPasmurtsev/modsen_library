server:
  port: 9002

spring:
  application:
    name: books-service
  datasource:
    url: jdbc:mysql://localhost:3306/books?createDatabaseIfNotExist=true
    username: root
    password: 1111
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true