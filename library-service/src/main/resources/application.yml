server:
  port: 9003

spring:
  application:
    name: library-service
  datasource:
    url: jdbc:mysql://localhost:3306/library?createDatabaseIfNotExist=true
    username: root
    password: 1111
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
